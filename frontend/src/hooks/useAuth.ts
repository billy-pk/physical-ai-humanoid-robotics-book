/**
 * Authentication hooks for Better Auth.
 * 
 * Re-exports Better Auth hooks with additional convenience hooks.
 */

export { useSession } from "../lib/auth";
export { useAuth } from "../contexts/AuthContext";
