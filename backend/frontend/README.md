# Frontend

This directory contains the Docusaurus frontend for the AI-Course-Book platform.

## Setup

1. Navigate into the `frontend/` directory.
2. Install dependencies: `npm install`

## Running the application

`npm start`
